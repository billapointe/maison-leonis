# Protection du site via page d'accès (index.html).
# Configurer GATE_PASSWORD dans Netlify : Site configuration → Environment variables.
# Pour ouvrir le site au public : supprimer GATE_PASSWORD et redéployer.

[build]
  command = "node scripts/generate-auth-config.mjs"
  publish = "."
